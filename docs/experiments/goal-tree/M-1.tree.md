# Célfa — M-1

> A stranger starts A-Team on their own repository and gets a live PM view in which each ticket's state and time-in-state are visible at a glance, with a cheap agent context.

**37 cél · 27 levél · 25h 50m becsült munka · 7 emberi döntés**

Forrás: `M-1.tree.json` · generálva: 2026-07-20T13:20:00+02:00

## A te döntéseid

Ezek nélkül az ügynök vagy áll, vagy kitalálja őket.

| # | cél | idő | mit kell eldöntened |
|---|---|---|---|
| `M-1.1.1` | The taxonomy config contract is decided and written into schemas/ | 45m | One schema file states where the project taxonomy lives, which keys it carries, and its version, so init-workspace, the board and prime read the same contract. |
| `M-1.3.1` | The prime entry mode is decided and recorded | 30m | skills/prime/SKILL.md states whether prime is injected at session start by a hook or invoked explicitly, and what a repository that cannot run hooks does instead. |
| `M-1.3.2` | The init-workspace boundary conflict is resolved and recorded | 30m | METHOD.md and skills/init-workspace/SKILL.md agree on whether initialisation may write an AGENTS.md pointer at all. |
| `M-1.4.1` | The shape of the milestone operations is decided | 45m | The package states whether milestones get their own skills or a milestone mode on the sprint operations, and the roster documents name the result. |
| `M-1.4.5` | The migration of this repository's own active sprint is decided and recorded | 30m | A written decision says what happens to a-team/sprint.md and its committed tickets when the milestone becomes the container here. |
| `M-1.5.1` | The review verdict gets one canonical durable record | 1h 00m | The event contract defines where a review verdict lives and what values it may take, so a consumer reads a field instead of parsing prose. |
| `M-1.5.3.1` | The provider token source is chosen and recorded in schemas/tokens.md | 45m | The token contract names the concrete surface an operation reads provider totals from, and what it does when that surface reports nothing. |

## Áttekintés

`★` a te döntésed · `▾` bontva · `·` levél

```
  25h 50m  ▾ M-1          The A-Team PM surface stands at Backlog.md level
   3h 05m    ▾ M-1.1        A fresh repository initialises into a project-shaped workspace
★     45m      · M-1.1.1      The taxonomy config contract is decided and written into schemas/
   1h 00m      · M-1.1.2      init-workspace writes the taxonomy config inside the one atomic bootstrap
   1h 00m      · M-1.1.3      The board's filters and labels are served from the taxonomy config
      20m      · M-1.1.4      One documented command starts the board from a fresh clone with no install step
   4h 30m    ▾ M-1.2        The board shows the whole lifecycle with time-in-state
      15m      · M-1.2.1      Lifecycle columns and age-in-state render from the event log
   1h 45m      ▾ M-1.2.2      A card opens a drawer carrying the full ticket contract
      45m        · M-1.2.2.1    The server serves one ticket's parsed body and its events
   1h 00m        · M-1.2.2.2    The drawer renders the contract and stays read-only
   1h 45m      ▾ M-1.2.3      A cumulative-flow view is derived from the event log
      15m        · M-1.2.3.1    The flow view's placement is decided and recorded in the board skill
   1h 30m        · M-1.2.3.2    /api/flow computes per-day per-status counts with no new dependency
      45m      · M-1.2.4      Every endpoint stays read-only and loopback-only
   2h 30m    ▾ M-1.3        prime is the documented default session context for agents
★     30m      · M-1.3.1      The prime entry mode is decided and recorded
★     30m      · M-1.3.2      The init-workspace boundary conflict is resolved and recorded
      30m      · M-1.3.3      The prime pointer exists in the shipped AGENTS.md and in a freshly initialised repository
      15m      · M-1.3.4      The routine-turn read path is measured against the full method read
      45m      · M-1.3.5      A prime-only session routes three routine requests to their owning operation
   9h 30m    ▾ M-1.4        The milestone is the working commitment container
★     45m      · M-1.4.1      The shape of the milestone operations is decided
   3h 00m      · M-1.4.2      A milestone is opened by an operation, on a clean baseline, exactly once
   2h 30m      · M-1.4.3      A milestone is closed only against goal evidence
   2h 15m      ▾ M-1.4.4      Velocity is reported per unit time under concurrent milestones
      45m        · M-1.4.4.1    The weekly velocity formula is written into schemas/metrics.md
   1h 30m        · M-1.4.4.2    report-metrics prints the weekly figure with its coverage
★     30m      · M-1.4.5      The migration of this repository's own active sprint is decided and recorded
      30m      · M-1.4.6      The hand-made M-1 record validates against what the operations produce
   6h 15m    ▾ M-1.5        Done cards carry the review verdict and the token cost
★  1h 00m      · M-1.5.1      The review verdict gets one canonical durable record
   2h 00m      · M-1.5.2      review-ticket emits the verdict record on every round
   2h 15m      ▾ M-1.5.3      A closed ticket carries provider-reported token totals
★     45m        · M-1.5.3.1    The provider token source is chosen and recorded in schemas/tokens.md
   1h 30m        · M-1.5.3.2    close-ticket appends token_usage with preserved categories
   1h 00m      · M-1.5.4      The Done column renders verdict and cost, and marks absence explicitly
```

## Részletek

### M-1 · The A-Team PM surface stands at Backlog.md level

A stranger starts A-Team on their own repository and gets a live PM view in which each ticket's state and time-in-state are visible at a glance, with a cheap agent context.

**25h 50m** · 5 gyerek · gazda: ügynök

*Miért nem levél:* c1 spans five separately buildable surfaces (project taxonomy at init, the board, prime, the milestone operations, done-card evidence). None of them can be observed before it exists.

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | A stranger with no A-Team knowledge reaches a rendered board of their own project in one documented sequence. | *folyamat* · `In `mktemp -d` + `git init`, run the init-workspace operation, capture two tickets, then run the start command copied verbatim out of skills/board/SKILL.md, and open the printed URL.`<br>→ The board renders lifecycle columns, per-card age-in-state, and this project's own lane/type labels; no method file was opened during the run. |
| c2 | No PM state changed outside an owning operation while the milestone ran. | *parancs* · `python3 -c "import json;[print(l) for l in open('a-team/metrics/events.jsonl') if json.loads(l).get('event','').startswith(('ticket_','milestone_','sprint_')) and not json.loads(l).get('source')]"`<br>→ empty output — every lifecycle line since the baseline names the operation that wrote it |
| c3 | The routine agent read path is a digest, not the method. | *mérés* · `python3 skills/prime/prime.py \| wc -l  vs  cat AGENTS.md METHOD.md PROCESSES.md schemas/*.md \| wc -l`<br>→ digest <= 70 lines against a >= 1200-line full read path (measured 33 vs 1246 at commit 5a076b5) |

Decomposed from the milestone's four Goal-evidence bullets plus the 'stranger on a fresh repo' framing in its goal sentence.

#### M-1.1 · A fresh repository initialises into a project-shaped workspace

init-workspace leaves a workspace whose lane/type taxonomy is the project's own, and the board is one documented command away.

**3h 05m** · 4 gyerek · gazda: ügynök

*Miért nem levél:* c1 cannot be observed until the taxonomy config contract exists and both init-workspace and the board read the same file.

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The board's filters offer exactly the lanes this project configured, never a built-in list. | *folyamat* · `In the temp repo initialised with prefix `X` and lanes `api,web`, start the board and open the lane select in the header.`<br>→ the select lists `api` and `web` and nothing else besides the empty 'all' entry |
| c2 | Initialisation still creates infrastructure only — no ticket, sprint, event, or priority. | *parancs* · `git -C "$T" status --porcelain`<br>→ only a-team/backlog.md, a-team/tickets/.gitkeep, a-team/metrics/events.jsonl and the config file appear; events.jsonl is zero bytes and a-team/sprint.md does not exist |

Depends on AT-1 (a-team/config.yml v1, ticket_prefix) landing first. AT-1 is `ready` in the 2026-07-19 sprint and carries no `milestone:` field — the milestone's first evidence bullet therefore rests on work that is not committed to it.

##### M-1.1.1 · The taxonomy config contract is decided and written into schemas/ ★

One schema file states where the project taxonomy lives, which keys it carries, and its version, so init-workspace, the board and prime read the same contract.

**45m** · levél · gazda: ember

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | A single schema file defines the taxonomy file, its lane and type keys, their display labels, and a schema version. | *parancs* · `grep -nE 'lanes\|types\|schema_version' schemas/config.md`<br>→ all three matched inside one schema file |
| c2 | The decision states whether taxonomy extends AT-1's a-team/config.yml v1 or lives in a separate file, and what a workspace initialised before it does. | *parancs* · `grep -n 'config.yml' schemas/config.md skills/init-workspace/SKILL.md`<br>→ both files name the same path and the same missing-taxonomy behaviour |

human: this fixes an on-disk contract that every consuming repository writes once at bootstrap. Changing lane/type keys afterwards is a migration across other people's repositories, not an edit.

##### M-1.1.2 · init-workspace writes the taxonomy config inside the one atomic bootstrap

A fresh initialisation produces the taxonomy config together with the three canonical artifacts, or writes nothing at all.

**1h 00m** · levél · gazda: ügynök · függ: `M-1.1.1`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | Initialisation obtains the taxonomy before writing and creates it with the other artifacts. | *folyamat* · `T=$(mktemp -d); git -C "$T" init; run the init-workspace operation there, answering the prefix and lane questions.`<br>→ the operation reports created and lists four artifacts |
| c2 | The bootstrap result is exactly the canonical set — nothing more, nothing partial. | *parancs* · `test -s "$T/a-team/config.yml" && test ! -s "$T/a-team/metrics/events.jsonl" && test ! -e "$T/a-team/sprint.md" && test -d "$T/a-team/tickets"; echo $?`<br>→ 0 |
| c3 | A refused initialisation leaves the repository byte-identical. | *parancs* · `Run the operation in a second temp repo without approving a taxonomy, then: git -C "$T2" status --porcelain`<br>→ empty output |

##### M-1.1.3 · The board's filters and labels are served from the taxonomy config

The lane and type vocabulary the board offers comes from the workspace config, not from values baked into board.html.

**1h 00m** · levél · gazda: ügynök · függ: `M-1.1.1`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | /api/state exposes the configured taxonomy. | *parancs* · `curl -s http://127.0.0.1:$P/api/state \| jq -r '.taxonomy.lanes[]'`<br>→ exactly the lanes listed in a-team/config.yml, in file order |
| c2 | No lane or type vocabulary remains hardcoded in the board page. | *parancs* · `grep -n '<option' skills/board/board.html`<br>→ the only literal options are the two empty 'all' entries; every other option is built from the fetched taxonomy |
| c3 | A workspace with no taxonomy config still renders. | *UI* · `Start the board on a workspace whose config file is absent and look at the header selects and the columns.`<br>→ the selects show only the 'all' entry, the columns still render, and no error banner appears |

##### M-1.1.4 · One documented command starts the board from a fresh clone with no install step

skills/board/SKILL.md carries a start command that a stranger can copy, and it needs nothing but a stock Node.js.

**20m** · levél · gazda: ügynök

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The documented command, copied verbatim, serves the board. | *parancs* · `Run the command from the Start block of skills/board/SKILL.md, then: curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:4400/`<br>→ 200 |
| c2 | The board pulls in no third-party package. | *parancs* · `grep -n '^import' skills/board/serve.mjs \| grep -v 'node:'; test ! -e package.json`<br>→ no import line survives the filter (verified at commit 5a076b5) and the package has no package.json |

#### M-1.2 · The board shows the whole lifecycle with time-in-state

Every lifecycle state is reachable on the board, each card carries the time it has spent in its current state, and a card can be opened for its full contract.

**4h 30m** · 4 gyerek · gazda: ügynök

*Miért nem levél:* c1 cannot be observed for the drawer and the cumulative-flow surface before either of them exists.

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | Every status the server can emit has a place on the screen. | *UI* · `Compare `curl -s http://127.0.0.1:$P/api/state \| jq -r '.lifecycle[]'` with the column headings rendered in #board.`<br>→ each status appears as a column heading; blocked/parked/rejected may be hidden only while their card count is zero |
| c2 | The board and prime derive the same time-in-state for the same ticket. | *parancs* · `diff <(python3 skills/prime/prime.py \| grep -oE '^  [A-Z0-9-]+' ) <(curl -s http://127.0.0.1:$P/api/state \| jq -r '.tickets[] \| "\(.id) \(.status_since)"') — compare the status_since behind each printed age`<br>→ for every ticket both tools use the same last lifecycle event timestamp |

Drag-and-drop and form editing are excluded by AT-8 on purpose: they would be ungated state writing.

##### M-1.2.1 · Lifecycle columns and age-in-state render from the event log

Cards sit in the column their last lifecycle event dictates and carry the age since that event; column headers carry the median.

**15m** · levél · gazda: ügynök

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | No ticket renders without a status_since. | *parancs* · `curl -s http://127.0.0.1:$P/api/state \| jq -r '.tickets[] \| select(.status_since==null) \| .id'`<br>→ empty output |
| c2 | The age shown is time in the current state, not time since ready_at or started_at. | *parancs* · `Pick a ticket whose frontmatter ready_at differs from its last event (AT-8: ready_at empty, last event 2026-07-20T12:39), then read its age in `python3 skills/prime/prime.py` and on its card.`<br>→ both show the age since 2026-07-20T12:39, not since created_at |
| c3 | Non-terminal column headers carry a median age. | *UI* · `Read the Backlog, Ready, In progress and Review column headings on the board.`<br>→ each shows `medián <age>`; Done/Parked/Rejected show none |

Already delivered under AT-8/AT-9. The node exists so the milestone re-observes its own evidence at close, not to redo the work.

##### M-1.2.2 · A card opens a drawer carrying the full ticket contract

A reader answers 'why is this ticket here and what has to be true to move it' from the board alone.

**1h 45m** · 2 gyerek · gazda: ügynök

*Miért nem levél:* c1 has nothing to read: /api/state returns a 200-character summary only (skills/board/serve.mjs:50), so no ticket body is available to any drawer.

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The drawer carries the ticket's contract and its event history. | *folyamat* · `Open the board, click the AT-9 card, and read the drawer without opening a-team/tickets/AT-9-pm-surface-review-defects.md.`<br>→ the drawer shows Outcome, Scope, Acceptance criteria, Verification and the ticket's lifecycle events with timestamps |

###### M-1.2.2.1 · The server serves one ticket's parsed body and its events

A per-ticket endpoint returns the ticket's markdown sections and its lifecycle events as JSON, under the same containment rules as the rest of the server.

**45m** · levél · gazda: ügynök

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The endpoint returns the ticket's sections keyed by heading. | *parancs* · `curl -s http://127.0.0.1:$P/api/ticket/AT-9 \| jq -r '.sections \| keys[]'`<br>→ includes Outcome, Scope, Acceptance criteria, Verification |
| c2 | The endpoint returns the ticket's lifecycle events in append order. | *parancs* · `curl -s http://127.0.0.1:$P/api/ticket/AT-9 \| jq -r '.events[-1].event'`<br>→ ticket_submitted_for_review |
| c3 | The ticket id cannot address a file outside the tickets directory. | *parancs* · `curl -s -o /dev/null -w '%{http_code}\n' 'http://127.0.0.1:'$P'/api/ticket/..%2f..%2fREADME.md'; curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:$P/api/ticket/nope`<br>→ 404 and 404 |

###### M-1.2.2.2 · The drawer renders the contract and stays read-only

Clicking a card opens a panel with the ticket's sections and history; nothing in it can write.

**1h 00m** · levél · gazda: ügynök · függ: `M-1.2.2.1`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | A click opens the drawer for that ticket and Escape closes it. | *UI* · `Click the AT-9 card on the board, then press Escape.`<br>→ a panel headed `AT-9` appears with the four sections and the event list; Escape returns to the columns with the previous filters intact |
| c2 | The page issues no mutating request. | *parancs* · `grep -nE "method:[ ]*['\"](POST\|PUT\|PATCH\|DELETE)" skills/board/board.html`<br>→ empty output |

##### M-1.2.3 · A cumulative-flow view is derived from the event log

The board shows how many tickets sat in each state per day, computed from events.jsonl alone.

**1h 45m** · 2 gyerek · gazda: ügynök

*Miért nem levél:* c1 has no /api/flow to call, and the view's placement is unsettled, so there is no screen to observe it on.

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The chart's last day agrees with the board's current column counts. | *mérés* · `curl -s http://127.0.0.1:$P/api/flow \| jq '.[-1]' against curl -s http://127.0.0.1:$P/api/state \| jq -r '.tickets[].status' \| sort \| uniq -c`<br>→ identical count per status, zero difference |

###### M-1.2.3.1 · The flow view's placement is decided and recorded in the board skill

skills/board/SKILL.md states where the flow view lives, so the implementation and the reader agree.

**15m** · levél · gazda: ügynök

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The skill names one placement. | *parancs* · `grep -n -i 'flow' skills/board/SKILL.md`<br>→ states either 'same page, below the columns' or 'second view at /flow', exactly one of the two |

Kept agent-owned although it is a UI taste call: the surface is a read-only local page in one HTML file, reversible in minutes. It is not worth a PO/PM interruption. Contrast with M-1.1.1, which writes files into other people's repositories.

###### M-1.2.3.2 · /api/flow computes per-day per-status counts with no new dependency

The server replays events.jsonl into a per-day status histogram using node: built-ins only.

**1h 30m** · levél · gazda: ügynök · függ: `M-1.2.3.1`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The endpoint returns one entry per day since the first event. | *parancs* · `curl -s http://127.0.0.1:$P/api/flow \| jq 'length'`<br>→ >= 2 for this package workspace, whose first event is 2026-07-19 |
| c2 | The replay honours the effective-event rules, not the raw lines. | *parancs* · `Append an `event_corrected` line voiding one ticket_ready in a temp workspace copy, restart the server, then: curl -s http://127.0.0.1:$P/api/flow \| jq '.[0]'`<br>→ the voided transition is absent from the histogram, per schemas/events.md 'Effective event view' |
| c3 | No third-party package entered the server. | *parancs* · `grep -n '^import' skills/board/serve.mjs \| grep -v 'node:'`<br>→ empty output |

##### M-1.2.4 · Every endpoint stays read-only and loopback-only

The endpoints added for the drawer and the flow view widen neither the served file set nor the write surface.

**45m** · levél · gazda: ügynök · függ: `M-1.2.2.1`, `M-1.2.3.2`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | Only board assets and the API answer; source and traversal do not. | *parancs* · `for p in / /board.html /serve.mjs /../README.md /.git/HEAD '/..%2f..%2fREADME.md'; do curl -s -o /dev/null -w "$p %{http_code}\n" "http://127.0.0.1:$P$p"; done`<br>→ / 200, /board.html 200, all others 404 |
| c2 | A mutating method is refused rather than silently answered. | *parancs* · `curl -s -o /dev/null -w '%{http_code}\n' -X POST http://127.0.0.1:$P/api/state`<br>→ 405 — at commit 5a076b5 this returns 200, because serve.mjs never branches on req.method |
| c3 | The server is unreachable off the loopback interface. | *parancs* · `curl -s -o /dev/null --max-time 3 -w '%{http_code}\n' "http://$(ipconfig getifaddr en0):$P/"`<br>→ 000 |

#### M-1.3 · prime is the documented default session context for agents

An agent's first read in this repository and in a consuming one is the prime digest, and nothing in the package contradicts that.

**2h 30m** · 5 gyerek · gazda: ügynök

*Miért nem levél:* c1 cannot be satisfied before the entry mode is chosen and the init-workspace boundary conflict is settled — until then it is unknown what the pointer should say or who writes it.

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The package's own entry documents name prime as the session entry. | *parancs* · `grep -n 'prime' AGENTS.md SKILL.md PROCESSES.md`<br>→ all three name prime as the first read of a session; at commit 5a076b5 AGENTS.md has no hit at all |
| c2 | No surviving instruction tells an agent to read the full method before a routine turn. | *parancs* · `grep -rn 'METHOD.md' AGENTS.md PROCESSES.md SKILL.md`<br>→ no hit that governs a routine turn; an operation skill may still require its own owning skill and the schemas it names |

##### M-1.3.1 · The prime entry mode is decided and recorded ★

skills/prime/SKILL.md states whether prime is injected at session start by a hook or invoked explicitly, and what a repository that cannot run hooks does instead.

**30m** · levél · gazda: ember

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | One entry mode is stated, with the fallback for hook-less repositories. | *parancs* · `grep -n -A3 -i 'entry mode' skills/prime/SKILL.md`<br>→ either 'hook-injected at session start' or 'explicit invocation', plus the named fallback |

human: a session-start hook commits every consuming repository to harness-specific configuration that lives outside the A-Team package, and it changes behaviour for sessions that never asked for it.

##### M-1.3.2 · The init-workspace boundary conflict is resolved and recorded ★

METHOD.md and skills/init-workspace/SKILL.md agree on whether initialisation may write an AGENTS.md pointer at all.

**30m** · levél · gazda: ember

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The Boundary section names AGENTS.md on exactly one side of its create/do-not-create split. | *parancs* · `grep -n 'AGENTS.md' skills/init-workspace/SKILL.md`<br>→ one hit inside either the 'Create only' or the 'Do not create' list, and none in both |
| c2 | METHOD.md's statement about what init-workspace may create matches the skill. | *parancs* · `grep -n 'init-workspace alone may create' METHOD.md`<br>→ the enumerated artifact list is the same list as the skill's Boundary section |

human: AT-7 asks init-workspace to install the prime pointer, while the skill's own Boundary (skills/init-workspace/SKILL.md:15-26) permits only backlog.md, tickets/ and events.jsonl, and METHOD.md:46 repeats it. Widening that boundary is a method change under an approval gate — the agent may not pick a side.

##### M-1.3.3 · The prime pointer exists in the shipped AGENTS.md and in a freshly initialised repository

Both the package's own AGENTS.md and whatever a fresh initialisation leaves behind point at prime as the session's first read.

**30m** · levél · gazda: ügynök · függ: `M-1.3.2`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The package's AGENTS.md points at prime. | *parancs* · `grep -n 'skills/prime' AGENTS.md`<br>→ at least one line naming prime as the first read of a session |
| c2 | A freshly initialised repository matches whatever M-1.3.2 decided. | *parancs* · `grep -c prime "$T/AGENTS.md" 2>&1`<br>→ count 1 if the M-1.3.2 decision installs the pointer; 'No such file' if it does not — and skills/init-workspace/SKILL.md states which of the two |

##### M-1.3.4 · The routine-turn read path is measured against the full method read

The digest cost and the method cost are both recorded numbers, and the ratio is at least tenfold.

**15m** · levél · gazda: ügynök

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The digest fits in a session preamble. | *mérés* · `python3 skills/prime/prime.py \| wc -l`<br>→ <= 70 (measured 33 at commit 5a076b5 on the package's own 13-ticket workspace) |
| c2 | The baseline it replaces is recorded, not asserted. | *mérés* · `cat AGENTS.md METHOD.md PROCESSES.md schemas/*.md \| wc -l`<br>→ >= 1200 (measured 1246 at commit 5a076b5); the ratio against c1 is >= 10x and both numbers are written into the ticket's Result |

Line count is a proxy for context cost, not a token measurement. If the milestone wants tokens, that is M-1.5.3's machinery, not this node's.

##### M-1.3.5 · A prime-only session routes three routine requests to their owning operation

An agent given nothing but the prime output names the right operation for the three most common plain-language requests, and touches no file directly.

**45m** · levél · gazda: ügynök

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | Each request reaches its owning operation without a method read. | *folyamat* · `In a fresh session whose only A-Team context is the output of `python3 skills/prime/prime.py`, issue in turn: 'írjuk fel ezt: <x>', 'mi jön most?', 'kész van az AT-9?'. Record the operation named before any file is touched.`<br>→ capture-work; the ready queue read out of the digest; review-ticket — and no direct edit of a ticket, backlog, sprint or events file |
| c2 | The digest carries the human gates it must never let an agent cross. | *parancs* · `python3 skills/prime/prime.py \| grep -A4 'HUMAN GATES'`<br>→ commitment, scope change after ready, accepting open findings, disposition and history correction, method/skill changes, and external actions all appear |

The softest check in the tree: c1 is one run, therefore one sample. It observes routing, not quality, and a second model may route differently.

#### M-1.4 · The milestone is the working commitment container

Milestones are opened and closed by operations that write both the file and the event; nobody edits milestone state by hand.

**9h 30m** · 6 gyerek · gazda: ügynök

*Miért nem levél:* c1 has no operation to run: `ls skills | grep milestone` returns nothing at commit 5a076b5.

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | A milestone can be taken through its whole life without a hand edit. | *folyamat* · `In a temp clone, run the milestone-opening operation on a throwaway goal, close it against evidence, then: git -C "$T" diff --stat`<br>→ only a-team/milestones/<id>-<slug>.md and a-team/metrics/events.jsonl changed, and both were written by the operations |
| c2 | The hand-made exception stays a single disclosed exception. | *parancs* · `grep -c 'office-hours-manual' a-team/metrics/events.jsonl`<br>→ 1 — the AT-8 milestone_started line, and no second one appears after the operations land |

AT-5's third open question (does retro attach to each milestone close or to a cadence?) is deliberately not decomposed here: retro cadence is outside this milestone's goal, which is the PM surface.

##### M-1.4.1 · The shape of the milestone operations is decided ★

The package states whether milestones get their own skills or a milestone mode on the sprint operations, and the roster documents name the result.

**45m** · levél · gazda: ember

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The chosen shape exists on disk in exactly one form. | *parancs* · `ls skills \| grep -E 'milestone'`<br>→ either two new skill directories (plan-milestone, close-milestone), or none plus a documented milestone mode in skills/plan-sprint/SKILL.md — not a mixture |
| c2 | Every roster names the same operations. | *parancs* · `grep -n -i 'milestone' METHOD.md PROCESSES.md SKILL.md`<br>→ METHOD.md's lifecycle-ownership table, PROCESSES.md and root SKILL.md list the identical operation names |

human: operation names are the router's public surface. Consuming repositories and their AGENTS.md files reference them by name, so a later rename breaks instructions the package does not own.

##### M-1.4.2 · A milestone is opened by an operation, on a clean baseline, exactly once

The opening operation writes a schema-conforming milestone file and one milestone_started event, refuses a dirty tree, and is idempotent by entity.

**3h 00m** · levél · gazda: ügynök · függ: `M-1.4.1`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The operation writes both the file and the event. | *folyamat* · `In a temp clone with a clean tree, run the milestone-opening operation with an approved goal and two committed ticket ids.`<br>→ a-team/milestones/<id>-<slug>.md carries the exact frontmatter keys of schemas/milestone.md, and one milestone_started line is appended |
| c2 | The event payload matches the schema. | *parancs* · `grep 'milestone_started' "$T/a-team/metrics/events.jsonl" \| tail -1 \| jq -r 'keys[]'`<br>→ includes milestone, goal, baseline_commit, committed_ticket_ids, timestamp, event, source |
| c3 | A dirty tree is a refusal, not a stash. | *parancs* · `touch "$T/dirt"; run the opening operation again for a second milestone; then: git -C "$T" status --porcelain`<br>→ the operation refuses naming the baseline prerequisite; status shows only `?? dirt`, and no second milestone file or event exists |
| c4 | Rerunning the same opening is semantically idempotent. | *parancs* · `Rerun the opening operation for the same milestone id, then: grep -c 'milestone_started' "$T/a-team/metrics/events.jsonl"`<br>→ 1 |

##### M-1.4.3 · A milestone is closed only against goal evidence

The closing operation refuses an empty Result, and on close writes the schema's payload including the per-container throughput.

**2h 30m** · levél · gazda: ügynök · függ: `M-1.4.1`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | Closure without evidence is refused without mutation. | *folyamat* · `Run the closing operation on the temp milestone whose `## Result` section is still empty.`<br>→ refusal naming the missing goal evidence; status stays active, closed_at stays empty, no milestone_closed line is appended |
| c2 | Closure with evidence writes the file state and the event together. | *folyamat* · `Fill `## Result` with the goal evidence and rerun the closing operation.`<br>→ frontmatter status: closed with closed_at set, and one milestone_closed line appended |
| c3 | The closing event carries the schema's payload. | *parancs* · `grep 'milestone_closed' "$T/a-team/metrics/events.jsonl" \| tail -1 \| jq -r 'keys[]'`<br>→ includes milestone, result, completed_committed_points, throughput |
| c4 | An abandoned milestone is a separate, provenanced outcome. | *parancs* · `Abandon a second temp milestone, then: grep 'milestone_closed' "$T/a-team/metrics/events.jsonl" \| tail -1 \| jq -r '.result'`<br>→ abandoned, with a reason and decision provenance recorded in the milestone's Result section |

##### M-1.4.4 · Velocity is reported per unit time under concurrent milestones

With several milestones active at once, the report gives one per-week velocity figure with its window, instead of per-container numbers that double-count overlapping containers.

**2h 15m** · 2 gyerek · gazda: ügynök

*Miért nem levél:* c1 has no formula to report against: schemas/metrics.md defines committed velocity per sprint only, and contains no per-week rule (grep 'per week' schemas/metrics.md is empty at commit 5a076b5).

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The report gives a per-week figure with a stated window and coverage. | *folyamat* · `Run the report-metrics operation on a workspace with two active milestones sharing a week.`<br>→ one weekly velocity figure naming its window and timezone, its included and excluded records, and no per-container velocity claim |

###### M-1.4.4.1 · The weekly velocity formula is written into schemas/metrics.md

One formula defines weekly velocity, its week boundary, and its timezone, so two reports of the same workspace produce the same number.

**45m** · levél · gazda: ügynök

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The formula and its boundary rule exist in the metrics schema. | *parancs* · `grep -n -i 'per week\\|weekly' schemas/metrics.md`<br>→ one formula plus the rule stating which event timestamp assigns a ticket to a week and in which timezone |
| c2 | The three files that mention per-unit-time velocity say the same thing. | *parancs* · `grep -n 'per unit time' METHOD.md schemas/milestone.md schemas/metrics.md`<br>→ all three point at the single formula in schemas/metrics.md and none restates it differently |

Agent-owned although it is a shared formula: METHOD.md and schemas/milestone.md already settle 'per week'. Only the boundary rule is open, and existing metrics conventions constrain it.

###### M-1.4.4.2 · report-metrics prints the weekly figure with its coverage

The reporting operation reads the formula from the schema and states period, records, exclusions and unavailable values.

**1h 30m** · levél · gazda: ügynök · függ: `M-1.4.4.1`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The report never turns a missing value into zero. | *folyamat* · `Run report-metrics on the package's own workspace, which has zero closed milestones and zero token_usage events.`<br>→ weekly velocity is shown as unavailable with the reason, not as 0; coverage lists what was excluded |
| c2 | The skill points at the formula rather than restating it. | *parancs* · `grep -n -i 'metrics.md' skills/report-metrics/SKILL.md`<br>→ the skill names schemas/metrics.md as the location of the weekly formula and carries no second copy of it |

##### M-1.4.5 · The migration of this repository's own active sprint is decided and recorded ★

A written decision says what happens to a-team/sprint.md and its committed tickets when the milestone becomes the container here.

**30m** · levél · gazda: ember

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The migration path is recorded where a reader will find it. | *parancs* · `grep -n -i 'migrat' PROCESSES.md METHOD.md`<br>→ states whether the 2026-07-19 sprint is closed first, runs to completion alongside the milestones, or is converted — one of the three, named |
| c2 | No ticket ends up in both containers. | *parancs* · `grep -l 'milestone: M-' a-team/tickets/*.md \| xargs grep -n '^sprint: .\+'`<br>→ empty output — schemas/milestone.md forbids both being non-empty |

human: it moves live commitment state and touches the running sprint's own metrics. METHOD.md forbids rewriting sprint history, so whichever path is chosen cannot be undone afterwards.

##### M-1.4.6 · The hand-made M-1 record validates against what the operations produce

The milestone file written by hand on 2026-07-20 has the same shape as an operation-produced one, or the difference is disclosed rather than discovered.

**30m** · levél · gazda: ügynök · függ: `M-1.4.2`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The frontmatter key set matches an operation-produced milestone. | *parancs* · `diff <(sed -n '2,9p' a-team/milestones/M-1-pm-surface.md \| cut -d: -f1) <(sed -n '2,9p' "$T/a-team/milestones/"*.md \| cut -d: -f1)`<br>→ empty diff — identical keys in identical order; only values differ |
| c2 | The manual creation stays disclosed in the record that claims it. | *parancs* · `grep -n 'office-hours-manual' a-team/metrics/events.jsonl; grep -n -i 'kézzel\\|by hand' a-team/tickets/AT-8-pm-surface-foundation.md`<br>→ exactly one event line, and AT-8's Result still states that M-1 was created by hand |

#### M-1.5 · Done cards carry the review verdict and the token cost

A card in the Done column shows how it was accepted and what it cost, or shows explicitly that the record does not exist.

**6h 15m** · 4 gyerek · gazda: ügynök

*Miért nem levél:* neither value has a canonical record today: schemas/events.md defines no review-verdict event, and `grep -c token_usage a-team/metrics/events.jsonl` returns 0. c1 has nothing to read.

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | Every done ticket renders a verdict and a cost, or an explicit absence marker. | *parancs* · `curl -s http://127.0.0.1:$P/api/state \| jq -r '.tickets[] \| select(.status=="done") \| "\(.id) \(.verdict) \(.tokens)"'`<br>→ every line carries a verdict value and a token value, or the literal absence marker; never 0 and never null for a missing record |
| c2 | What the card shows is what the event log holds. | *parancs* · `python3 -c "import json,urllib.request;s=json.load(urllib.request.urlopen('http://127.0.0.1:$P/api/state'));ev=[json.loads(l) for l in open('a-team/metrics/events.jsonl')];print([t['id'] for t in s['tickets'] if t['status']=='done' and t.get('tokens') not in (None,'—') and t['tokens']!=sum(e.get('reported_total_tokens') or 0 for e in ev if e.get('event')=='token_usage' and e.get('ticket_id')==t['id'])])"`<br>→ empty list — no card disagrees with the summed events |

##### M-1.5.1 · The review verdict gets one canonical durable record ★

The event contract defines where a review verdict lives and what values it may take, so a consumer reads a field instead of parsing prose.

**1h 00m** · levél · gazda: ember

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The event contract carries the verdict record and its payload. | *parancs* · `grep -n -E 'verdict\|ticket_reviewed' schemas/events.md`<br>→ one row defining the record with required payload including ticket_id, the review round, and the allowed verdict values |
| c2 | review-ticket writes to that record and to no parallel store. | *parancs* · `grep -n -i 'verdict' skills/review-ticket/SKILL.md`<br>→ the skill names that one record as the place the verdict is written |
| c3 | History written before the record existed stays visibly absent, not backfilled. | *parancs* · `grep -n -i 'verdict' schemas/events.md \| grep -n -i 'before\\|existing\\|earlier'`<br>→ the schema states that rounds recorded before this row was added carry no verdict and are not reconstructed |

human: it adds a row to the append-only event contract. Every existing consumer and every already-written history line is affected, and events are never rewritten — so a wrong shape is permanent.

##### M-1.5.2 · review-ticket emits the verdict record on every round

Each review round leaves one machine-readable verdict, and its round identity is unambiguous.

**2h 00m** · levél · gazda: ügynök · függ: `M-1.5.1`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | An accepted round writes one verdict record. | *folyamat* · `In a temp clone, run review-ticket on a ticket in review and accept it.`<br>→ one verdict record appended carrying the round number and the accept value; no second record for the same round |
| c2 | A rework round writes its own verdict, distinguishable from the accept. | *parancs* · `python3 -c "import json,collections;ev=[json.loads(l) for l in open('\"$T\"/a-team/metrics/events.jsonl')];c=collections.Counter((e.get('ticket_id'),e.get('round')) for e in ev if 'verdict' in e);print([k for k,v in c.items() if v>1])"`<br>→ empty list — one verdict per (ticket, round) |
| c3 | Rounds that predate the schema row are reported as absent, not invented. | *parancs* · `python3 -c "import json;ev=[json.loads(l) for l in open('a-team/metrics/events.jsonl')];sub={e['ticket_id'] for e in ev if e.get('event')=='ticket_submitted_for_review'};ver={e.get('ticket_id') for e in ev if 'verdict' in e};print(sorted(sub-ver))"`<br>→ only the tickets whose rounds happened before the schema change (AT-8, AT-9, TEMP-20260719-01 as of commit 5a076b5) |

##### M-1.5.3 · A closed ticket carries provider-reported token totals

Closing a ticket leaves a token_usage record with provider-reported numbers, or an explicit unavailability that no report turns into a zero.

**2h 15m** · 2 gyerek · gazda: ügynök

*Miért nem levél:* c1 has nothing to sum: `grep -c token_usage a-team/metrics/events.jsonl` is 0, and no provider surface is wired for close-ticket to read totals from.

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | Token figures on the board equal the sum of the ticket's token events. | *parancs* · `python3 -c "import json;ev=[json.loads(l) for l in open('a-team/metrics/events.jsonl')];print({e['ticket_id']:e.get('reported_total_tokens') for e in ev if e.get('event')=='token_usage'})" and compare with curl -s http://127.0.0.1:$P/api/state \| jq -r '.tickets[] \| "\(.id) \(.tokens)"'`<br>→ equal per ticket; a ticket with no token event shows the absence marker on both sides |

###### M-1.5.3.1 · The provider token source is chosen and recorded in schemas/tokens.md ★

The token contract names the concrete surface an operation reads provider totals from, and what it does when that surface reports nothing.

**45m** · levél · gazda: ember

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The schema names the surface and the unavailable case. | *parancs* · `grep -n -E 'source\|unavailable\|null' schemas/tokens.md`<br>→ names the concrete provider or harness surface, and restates that an unavailable value stays null and is never estimated or prorated |
| c2 | close-ticket reads that surface and nothing else. | *parancs* · `grep -n -i 'token' skills/close-ticket/SKILL.md`<br>→ the closure step names the same surface as schemas/tokens.md and offers no alternative estimate |

human: it commits the package to a provider/harness surface it does not own. A wrong choice writes unverifiable numbers into append-only history, and the method's own token metrics are a headline claim.

###### M-1.5.3.2 · close-ticket appends token_usage with preserved categories

Closure leaves one token_usage line whose keys match the token contract, with unknown fields null.

**1h 30m** · levél · gazda: ügynök · függ: `M-1.5.3.1`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | A closure with the surface available appends one conforming line. | *folyamat* · `In a temp clone, close a ticket that is in review with an accepted verdict, with the chosen provider surface reachable.`<br>→ one token_usage line appended, purpose taken from the schemas/tokens.md list, no duplicate for the same closure |
| c2 | The line's key set is exactly the token contract's. | *parancs* · `grep 'token_usage' "$T/a-team/metrics/events.jsonl" \| tail -1 \| jq -r 'keys[]' \| sort`<br>→ matches the field list in schemas/tokens.md; every value the provider did not report is null |
| c3 | A closure with no provider totals appends no invented numbers. | *parancs* · `Close a second ticket with the surface unreachable, then: grep -c 'token_usage' "$T/a-team/metrics/events.jsonl"`<br>→ the count is unchanged, and the closure report states that token accounting was unavailable |

##### M-1.5.4 · The Done column renders verdict and cost, and marks absence explicitly

Done cards carry a verdict badge and a token figure, and a missing record renders as an absence marker rather than a zero.

**1h 00m** · levél · gazda: ügynök · függ: `M-1.5.2`, `M-1.5.3.2`

| # | állítás | ellenőrzés |
|---|---|---|
| c1 | The Done column shows both values per card. | *UI* · `Open the board and read the cards in the Done column.`<br>→ each card shows a verdict badge (accepted / accepted-after-rework) and a token figure; a card without records shows `—` in both places |
| c2 | A missing total never renders as zero. | *parancs* · `curl -s http://127.0.0.1:$P/api/state \| jq -r '.tickets[] \| select(.status=="done") \| .tokens' \| grep -c '^0$'`<br>→ 0 |
| c3 | The board still writes nothing to show them. | *parancs* · `curl -s -o /dev/null -w '%{http_code}\n' -X POST http://127.0.0.1:$P/api/state; git status --porcelain a-team/`<br>→ 405 and empty output |

