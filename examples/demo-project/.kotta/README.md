# Demo workspace

This is the final canonical snapshot of one partially completed package. A
ticket appears in exactly one lifecycle directory, so earlier states are
described in the ticket history rather than duplicated as files.

The example demonstrates that human intent becomes a defined ticket, execution
is isolated by Git, and discovered work is captured as a finding before a human
turns it into backlog work.
